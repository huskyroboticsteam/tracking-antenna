#include <iostream>
#include <cmath>

int main(int argc, char** argv){
  // FILE* signal_strength_ptr = popen("iw wlan0 station dump | grep -E "signal:\s*" | tr -d -c "\- 0-9"");

  return EXIT_SUCCESS;
}